/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.netlify
next-env.d.ts
*.tsbuildinfo
.vercel
/test-results/
/playwright-report/
/blob-report/

# Lighthouse raw reports — large, regenerate rather than commit
docs/superpowers/baseline/*.json

# Hero concept measurement output — PNG frame captures and run JSON, written by
# scripts/measure-hero.mjs. Same rule as the Lighthouse reports: the numbers that
# matter are transcribed into MIGRATION.md, and the raw output regenerates.
/.hero-measure/

# Admin secrets, generated by scripts/gen-admin-secrets.mjs
.env.admin.local
.env.local

# Playwright storage state holds a live admin session cookie
tests/.auth/
